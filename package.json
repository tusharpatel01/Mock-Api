{
  "name": "pixisphere-mock-api",
  "version": "1.0.0",
  "scripts": {
 "start": "json-server --watch db.json --port $PORT --middlewares cors.js"

  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
